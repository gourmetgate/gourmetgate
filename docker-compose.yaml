services:
  database:
    image: postgres:16.9
    container_name: gourmetgate_db
    restart: always
    ports:
      - '15432:5432'
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
